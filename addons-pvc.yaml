apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: addons-pvc
  labels:
    app: odoo
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: csi-s3-b 
